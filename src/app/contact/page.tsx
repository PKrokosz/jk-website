export const metadata = {
  title: "Contact"
};

export default function ContactPage() {
  return (
    <main className="page" aria-labelledby="contact-heading">
      <section className="section" role="presentation">
        <div className="container">
          <h1 id="contact-heading">Kontakt</h1>
          <p>Tu wkrótce dodamy formularz oraz szczegóły dotyczące konsultacji.</p>
        </div>
      </section>
    </main>
  );
}
